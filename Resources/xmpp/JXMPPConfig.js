JXMPP_HAVEKEYS = true;          // whether to use keys
JXMPP_NKEYS    = 16;            // number of keys to generate
JXMPP_INACTIVITY = 300;         // qnd hack to make suspend/resume 
                                    // work more smoothly with polling
JXMPP_ERR_COUNT = 10;           // number of retries in case of connection
                                    // errors

JXMPP_ALLOW_PLAIN = true;       // whether to allow plaintext logins

JXMPP_CHECKQUEUEINTERVAL = 1;   // msecs to poll send queue
JXMPP_CHECKINQUEUEINTERVAL = 1; // msecs to poll incoming queue
JXMPP_TIMERVAL = 2000;          // default polling interval
JXMPP_RETRYDELAY = 5000;        // msecs to wait before trying next request after error
/*** END CONFIG ***/

